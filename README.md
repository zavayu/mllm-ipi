# IPI-Qwen

Local, reproducible evaluation pipeline scaffold for image-based prompt injection research using Qwen vision-language models.

This initial setup only includes the Python package structure and CLI foundation. Image rendering, Qwen integration, SAM integration, and Streamlit are intentionally out of scope for now.

## Usage

```bash
python -m src.main --help
```

## Tests

```bash
pytest
```
